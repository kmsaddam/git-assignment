# git-assignment
Assignment on module 1. Topic of the assignment is git repository creation.

# verify git-version
git --version

# Initialize Command
git init

# Track Files Command
git add hello.txt
git add *

# Commit Command
git commit -m "Test Git Files"

# Check Branch Name
git branch

# Modify Branch Name
git branch -M "dev"

# Modify Branch Name
git branch -M "dev"

# Connect with git repo 
git remote add origin https://github.com/kmsaddam/git-assignment.git

# Git Push Command 
git push origin dev

# Frequently Push Command  
git push -u origin dev

# Pull Command  
git pull origin dev

# Frequently Pull Command  
git pull origin dev







